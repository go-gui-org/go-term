//go:build windows

package main

// defaultFontFamily is the JetBrains Mono Nerd Font family as registered by the
// Windows Nerd Font installer (abbreviated naming). "NFM" is the Mono variant,
// whose icon glyphs stay single-width so the terminal grid does not break.
const defaultFontFamily = "JetBrainsMono NFM"
